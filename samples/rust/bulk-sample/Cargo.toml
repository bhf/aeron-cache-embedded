[package]
name = "bulk-sample"
version = "0.1.0"
edition = "2021"

[dependencies]
aeron-cache-embedded-client = { path = "../../../libraries/rust" }
tokio = { version = "1.0", features = ["full"] }
uuid = { version = "1.0", features = ["v4"] }
