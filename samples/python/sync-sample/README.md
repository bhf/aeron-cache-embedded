# Python Sync Sample

This sample demonstrates using the client in a synchronous manner (awaiting every call sequentially).

## Prerequisites

- Python 3.7+
- Poetry

## Running the Sample

1. Install dependencies:
   ```bash
   poetry install
   ```

2. Run the sample:
   ```bash
   poetry run python sync_sample.py http://localhost:8080
   ```
