# Python Async Sample

This sample demonstrates using `asyncio.create_task` to perform cache operations asynchronously.

## Prerequisites

- Python 3.7+
- Poetry

## Running the Sample

1. Install dependencies:
   ```bash
   poetry install
   ```

2. Run the sample:
   ```bash
   poetry run python async_sample.py http://localhost:8080
   ```
